const helpers = {}

helpers.placeholderImg = function(gender) {
   return `<img src="./assets/backup-head-${gender}.png" alt="Placeholder avatar image of a blank silhouette." class="placeholder">`
}

export default helpers